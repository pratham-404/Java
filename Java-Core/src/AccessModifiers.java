
// class 
    // 1. final
    // 2. abstract
    // 3. *public => accessible in any package
    // 4. default(if not mentioned anything) => only accessible in same package

// fields(variables)/methods(functions)
    // 1. *public => accessible in any class or package
    // 2. protected => only accessible in subsiding class(extends[Inheritance])
    // 3. private => only accessible in same class
    // 4. default(if not mentioned anything) => only accessible in same package 

public class AccessModifiers {

}
